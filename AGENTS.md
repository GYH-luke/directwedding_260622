# Agent Instructions

Before editing this landing page, read `PROJECT_MEMORY.md`.

Key reminders:

- Main work target is usually `index.html`.
- Keep the existing deep rose / pink / ivory tone.
- Upload `index.html` and the full `images/` folder together when deploying.
- Partner strip images are referenced from `images/partners-*.png`.
- Mobile layouts must avoid being covered by the floating bottom CTA.
- Remaining team counts are synced through `syncRemain(val)`.
